# Bugs

- Map invalid data error to some 4xx response

# Improvements

- Consider making `TestRequest::send` return `(Value, Status, Headers)` so we don't have to call `body_json` all the time.

# New features

- Timeline
    - Include tweets from yourself
    - And those you follow
- Mentions
